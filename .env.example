# Optional credentials. Copy to .env and fill only the values you use.
HF_TOKEN=
WANDB_API_KEY=

# Optional path/runtime overrides.
# HF_HOME=/data/github/SAO/.cache/huggingface
# RUNS=/data/github/SAO/runs
# CUDA_HOME defaults to the cu130 toolkit installed inside .venv.
